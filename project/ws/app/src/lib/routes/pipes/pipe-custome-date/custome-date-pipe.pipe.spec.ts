import { CustomeDatePipePipe } from './custome-date-pipe.pipe'

describe('CustomeDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomeDatePipePipe()
    expect(pipe).toBeTruthy()
  })
})
